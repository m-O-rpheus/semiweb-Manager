

    // --------------------------------------------------------------------------------------------------
    const funcwysiwyg_htmlspecialchars = function(str) {

        return str.replace(/[&<>'"]/g, function(x) {

            return '&#' + x.charCodeAt(0) + ';';
        });
    };
    // --------------------------------------------------------------------------------------------------

